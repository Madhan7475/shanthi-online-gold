# MongoDB
MONGO_URI=mongodb+srv://USERNAME:PASSWORD@CLUSTER.mongodb.net/DBNAME?retryWrites=true&w=majority

# JWT (used for local OTP/JWT auth path)
JWT_SECRET=change_this_to_a_long_random_string

# CORS: Comma-separated list of allowed origins (no spaces unless you intend them)
# Examples:
#   For Vercel preview + production + local:
#   https://your-app.vercel.app,https://your-app-git-*.vercel.app,http://localhost:5173
CORS_ORIGIN=http://localhost:5173

# Firebase Admin (service account) — store as environment variables on Render
# IMPORTANT:
# - Do not include surrounding quotes in the values on Render.
# - For FIREBASE_PRIVATE_KEY: paste the key with literal \n for newlines (single line),
#   e.g. -----BEGIN PRIVATE KEY-----\nABC...\n-----END PRIVATE KEY-----\n
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxx@your-firebase-project-id.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\nREDACTED\n-----END PRIVATE KEY-----\n

# Razorpay (if using online payments)
RAZORPAY_KEY_ID=your_key_id
RAZORPAY_KEY_SECRET=your_key_secret

# Optional: override PORT if needed (Render sets PORT automatically)
# PORT=10000
