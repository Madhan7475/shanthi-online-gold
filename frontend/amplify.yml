version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend
        - nvm install 22.17.1
        - nvm use 22.17.1
        - corepack enable
    build:
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend/node_modules/**/*
