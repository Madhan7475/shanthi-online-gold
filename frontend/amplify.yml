version: 1
frontend:
  phases:
    preBuild:
      commands:
        - rm -rf node_modules
        - npm ci --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 
